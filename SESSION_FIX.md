# Исправление сохранения сессии

## Проблема

Сессия не сохраняется при обновлении страницы - нужно заново логиниться.

## ✅ Решение

Исправлена настройка Supabase клиента для явного сохранения сессии в localStorage.

### Что было исправлено:

1. **Настройка Supabase клиента:**
   - Включено `persistSession: true` - сессия сохраняется в localStorage
   - Включено `autoRefreshToken: true` - автоматическое обновление токена
   - Настроено `storage: window.localStorage` - явное указание хранилища

2. **Обработка событий аутентификации:**
   - Добавлена обработка `INITIAL_SESSION` - загрузка сессии при старте
   - Добавлена обработка `TOKEN_REFRESHED` - обновление токена
   - Добавлено логирование для отладки

## 🔍 Проверка

После деплоя проверьте:

1. Войдите в систему
2. Откройте DevTools (F12) → Application → Local Storage
3. Найдите ключ `supabase.auth.token` - должна быть сохранена сессия
4. Обновите страницу (F5)
5. Сессия должна сохраниться, и вы останетесь залогинены

## 🐛 Если проблема сохраняется

1. **Очистите localStorage:**
   - Откройте DevTools (F12) → Application → Local Storage
   - Удалите все ключи, связанные с Supabase
   - Попробуйте войти снова

2. **Проверьте консоль:**
   - Откройте DevTools (F12) → Console
   - Проверьте сообщения "Auth state changed"
   - Убедитесь, что нет ошибок

3. **Проверьте переменные окружения:**
   - Убедитесь, что в Vercel правильные переменные
   - URL должен быть: `https://nhsnxypdprellsmouhlp.supabase.co`
   - Anon ключ должен быть правильным

## 📋 Что происходит при входе

1. Пользователь вводит email и пароль
2. Supabase создает сессию
3. Сессия сохраняется в `localStorage` под ключом `supabase.auth.token`
4. При обновлении страницы:
   - Supabase читает сессию из localStorage
   - Проверяет валидность токена
   - Если токен валиден - пользователь остается залогинен
   - Если токен истек - автоматически обновляется (если `autoRefreshToken: true`)

## ⚠️ Важно

- Сессия хранится в localStorage браузера
- При очистке cookies/localStorage сессия будет потеряна
- Токены автоматически обновляются каждые ~1 час



