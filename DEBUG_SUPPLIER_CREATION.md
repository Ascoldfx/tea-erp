# Диагностика: Создание поставщика не работает

## ✅ Что было исправлено

1. **Улучшена генерация кода для кириллицы** - теперь "пробки" корректно преобразуется в код
2. **Добавлена проверка уникальности кода** - если код уже существует, добавляется суффикс
3. **Улучшено логирование ошибок** - теперь в консоли видно детали ошибки
4. **Улучшены сообщения об ошибках** - показываются детали и подсказки

---

## 🔍 Как проверить проблему

### Шаг 1: Проверьте, что таблица существует

1. Откройте SQL Editor:
   https://supabase.com/dashboard/project/nhsnxypdprellsmouhlp/sql/new

2. Выполните:
```sql
SELECT * FROM contractors LIMIT 1;
```

**Если ошибка "relation does not exist":**
- Выполните миграцию `migrations/create_contractors_table.sql`

### Шаг 2: Проверьте RLS политики

Выполните в SQL Editor:
```sql
SELECT * FROM pg_policies WHERE tablename = 'contractors';
```

Должны быть 4 политики:
- "Authenticated users can view contractors"
- "Authenticated users can create contractors"
- "Authenticated users can update contractors"
- "Authenticated users can delete contractors"

**Если политик нет:**
- Выполните миграцию `migrations/create_contractors_table.sql` (часть с политиками)

### Шаг 3: Проверьте консоль браузера

1. Откройте приложение
2. Нажмите **F12** (или Cmd+Option+I на Mac)
3. Перейдите на вкладку **Console**
4. Попробуйте создать поставщика
5. Посмотрите ошибки в консоли

**Типичные ошибки:**

- `relation "contractors" does not exist` → Таблица не создана, выполните миграцию
- `new row violates row-level security policy` → Проблема с RLS политиками
- `duplicate key value violates unique constraint "contractors_code_key"` → Код уже существует (должно исправляться автоматически)
- `permission denied for table contractors` → Проблема с правами доступа

### Шаг 4: Проверьте авторизацию

Убедитесь, что вы авторизованы:
- В консоли браузера выполните:
```javascript
localStorage.getItem('sb-nhsnxypdprellsmouhlp-auth-token')
```
- Должен вернуться токен (не null)

---

## 🛠️ Быстрое исправление

Если ничего не помогает, выполните полную миграцию:

1. Откройте SQL Editor:
   https://supabase.com/dashboard/project/nhsnxypdprellsmouhlp/sql/new

2. Выполните:
```sql
-- Проверка существования таблицы
SELECT EXISTS (
    SELECT FROM information_schema.tables 
    WHERE table_schema = 'public' 
    AND table_name = 'contractors'
);

-- Если false, выполните миграцию create_contractors_table.sql
```

3. После выполнения миграции попробуйте создать поставщика снова

---

## 📋 Что делать дальше

1. **Откройте консоль браузера (F12)**
2. **Попробуйте создать поставщика**
3. **Скопируйте ошибку из консоли** и сообщите мне

Улучшенный код теперь показывает детальные ошибки, что поможет быстро найти проблему.

---

## ✅ Проверка после исправления

После выполнения миграции и обновления кода:

1. Обновите страницу в браузере (Ctrl+R или Cmd+R)
2. Попробуйте создать поставщика "пробки"
3. Проверьте консоль - должны быть логи:
   - `Creating supplier with: {...}`
   - `Supplier created successfully: {...}`

Если ошибка все еще есть, скопируйте полный текст ошибки из консоли.



