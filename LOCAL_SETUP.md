# Инструкция для локального запуска проекта

## Быстрый старт

### 1. Создайте файл `.env` в корне проекта

Скопируйте содержимое из `env.txt` в новый файл `.env`:

```bash
cp env.txt .env
```

Или создайте файл `.env` вручную со следующим содержимым:
```env
VITE_SUPABASE_URL=https://eopxmdywmjvpffcnlwck.supabase.co
VITE_SUPABASE_ANON_KEY=sb_secret_pZM7M_11N_tn5CiT89X-BQ_U334SqGP
```

### 2. Установите зависимости

```bash
npm install
```

**Примечание:** Если видите предупреждения о версии Node.js (требуется >=20), это не критично для разработки. Для production рекомендуется обновить Node.js до версии 20+.

### 3. Запустите dev сервер

```bash
npm run dev
```

Приложение будет доступно по адресу: **http://localhost:5173**

### 4. Откройте в браузере

Откройте браузер и перейдите по адресу, который покажет Vite (обычно `http://localhost:5173`)

## Отладка

### Консоль браузера (F12)

Откройте DevTools (F12) и перейдите на вкладку **Console** для просмотра логов:

**При импорте техкарт:**
- `[parseTechCardsFromExcel] 🔍 Parsing monthly norms...` - начало парсинга
- `[parseTechCardsFromExcel] First 30 headers: ...` - заголовки колонок
- `[parseTechCardsFromExcel] ✅ Found date column X: "01.12.2025"` - найдены колонки с датами
- `[parseTechCardsFromExcel] ✅ Parsed monthly norm: ...` - нормы распарсены
- `[parseTechCardsFromExcel] 📊 Total monthly norms parsed: X` - итоговое количество

**При сохранении в БД:**
- `[RecipesService] ✅ Ингредиент X имеет Y норм по месяцам` - нормы сохранены

**При загрузке из БД:**
- `[RecipesService] ✅ Loaded X monthly norms...` - нормы загружены
- `[RecipeDetailsModal] ✅ Ingredient ... has X monthly norms` - нормы есть в UI

### Проверка переменных окружения

В консоли браузера можно проверить, что переменные окружения загружены:
```javascript
console.log(import.meta.env.VITE_SUPABASE_URL)
```

## Полезные команды

- `npm run dev` - запуск dev сервера с hot reload
- `npm run build` - сборка production версии
- `npm run preview` - предпросмотр production сборки
- `npm run lint` - проверка кода линтером

## Решение проблем

### Проблема: "Cannot find module"
**Решение:** 
```bash
rm -rf node_modules package-lock.json
npm install
```

### Проблема: "Supabase connection error"
**Решение:** 
1. Проверьте, что файл `.env` существует в корне проекта
2. Проверьте, что переменные начинаются с `VITE_`
3. Перезапустите dev сервер после создания/изменения `.env`

### Проблема: Порт 5173 занят
**Решение:** Vite автоматически предложит использовать другой порт, или можно указать вручную:
```bash
npm run dev -- --port 3000
```

### Проблема: "EPERM: operation not permitted"
**Решение:** 
```bash
sudo chown -R $(whoami) node_modules
npm install
```

## Структура проекта

- `src/` - исходный код приложения
- `src/pages/production/` - страницы производства (техкарты, планирование)
- `src/services/` - сервисы для работы с API
- `src/lib/supabase.ts` - настройка Supabase клиента
- `migrations/` - SQL миграции для базы данных

## Что проверить при отладке норм

1. **При импорте техкарт:**
   - Откройте консоль (F12)
   - Импортируйте техкарты из Excel
   - Проверьте логи `[parseTechCardsFromExcel]`
   - Должны быть видны колонки с датами `01.12.2025`, `01.01.2026` и т.д.

2. **При открытии техкарты:**
   - Откройте консоль (F12)
   - Откройте техкарту (двойной клик)
   - Проверьте логи `[RecipeDetailsModal]`
   - Должны быть видны нормы по месяцам для каждого ингредиента

3. **Если нормы не отображаются:**
   - Проверьте логи сохранения: `[RecipesService] ✅ Ингредиент X имеет Y норм`
   - Проверьте логи загрузки: `[RecipesService] ✅ Loaded X monthly norms`
   - Проверьте, что миграция `add_monthly_norms_to_recipe_ingredients.sql` выполнена в Supabase

## Дополнительная информация

- Документация Vite: https://vitejs.dev/
- Документация React: https://react.dev/
- Документация Supabase: https://supabase.com/docs
