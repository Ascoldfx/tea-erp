# Исправление: Создание поставщиков и пользователей

## ✅ Проблема 1: Ошибка при создании поставщика

**Причина:** Таблица `contractors` не существует в базе данных.

**Решение:** Выполните миграцию:

1. Откройте SQL Editor в Supabase:
   https://supabase.com/dashboard/project/nhsnxypdprellsmouhlp/sql/new

2. Скопируйте и выполните содержимое файла:
   `migrations/create_contractors_table.sql`

3. После выполнения миграции создание поставщиков через UI будет работать.

---

## ✅ Проблема 2: Не могу добавлять пользователей через сайт

**Причина:** Создание пользователей требует **Edge Functions**, которые не развернуты на сервере.

**Текущее решение (работает сейчас):**

### Способ 1: Через Supabase Dashboard

1. **Создайте пользователя:**
   - Откройте: https://supabase.com/dashboard/project/nhsnxypdprellsmouhlp/auth/users
   - Нажмите **"Add user"** → **"Create new user"**
   - Введите email и пароль
   - ✅ Включите **"Auto Confirm User"**

2. **Назначьте роль через SQL:**
   - Откройте SQL Editor: https://supabase.com/dashboard/project/nhsnxypdprellsmouhlp/sql/new
   - Выполните (замените `user@example.com` на email пользователя):

```sql
INSERT INTO profiles (id, email, full_name, role, is_active)
SELECT 
    id,
    email,
    'Имя Пользователя',  -- Замените на реальное имя
    'admin',  -- или 'procurement', 'production_planner', 'warehouse', 'director'
    true
FROM auth.users
WHERE email = 'user@example.com'
ON CONFLICT (id) 
DO UPDATE SET 
    role = 'admin',
    full_name = 'Имя Пользователя',
    is_active = true;
```

### Способ 2: Развернуть Edge Functions (для будущего)

Если хотите создавать пользователей через UI, нужно развернуть Edge Functions:

1. Следуйте инструкциям в `EDGE_FUNCTIONS_SETUP.md`
2. После развертывания создание через UI будет работать

---

## 📋 Что было исправлено

1. ✅ Улучшена обработка ошибок при создании поставщиков (теперь показывается детальное сообщение)
2. ✅ Добавлена генерация уникального ID для поставщиков
3. ✅ Создана миграция для таблицы `contractors`

---

## 🚀 Следующие шаги

1. **Выполните миграцию** `create_contractors_table.sql` в Supabase
2. **Проверьте создание поставщиков** через UI
3. **Для пользователей:** Используйте Dashboard + SQL (или разверните Edge Functions)

Подробнее в `USER_CREATION_FIX.md`.


