# Исправления, примененные к проекту

## ✅ Исправленные проблемы

### 1. SQL для создания/обновления профиля админа
**Файл:** `migrations/fix_admin_profile.sql`

Используйте этот SQL в Supabase SQL Editor для исправления вашего профиля:
```sql
INSERT INTO profiles (id, email, full_name, role)
SELECT id, email, 'Антон', 'admin'
FROM auth.users
WHERE email = 'ascoldfx@gmail.com'
ON CONFLICT (id) 
DO UPDATE SET role = 'admin', full_name = COALESCE(profiles.full_name, 'Антон'), is_active = true;
```

### 2. Реальная Supabase аутентификация
**Файлы:** 
- `src/context/AuthContext.tsx` - теперь использует реальную Supabase сессию
- `src/pages/auth/LoginPage.tsx` - форма email/password вместо Google OAuth
- `src/components/auth/ProtectedRoute.tsx` - учитывает состояние загрузки

**Что изменилось:**
- Сессия сохраняется между перезагрузками страницы
- Автоматическая проверка сессии при загрузке приложения
- Реальная аутентификация через email/password

### 3. Edge Functions для создания пользователей
**Файлы:**
- `supabase/functions/create-user/index.ts` - создание пользователей
- `supabase/functions/reset-password/index.ts` - сброс паролей
- `src/services/usersService.ts` - обновлен для использования Edge Functions

**Важно:** Edge Functions нужно развернуть! См. `EDGE_FUNCTIONS_SETUP.md`

## 🚀 Что нужно сделать дальше

### Шаг 1: Исправьте профиль админа
Выполните SQL из `migrations/fix_admin_profile.sql` в Supabase SQL Editor.

### Шаг 2: Разверните Edge Functions (опционально, но рекомендуется)
Следуйте инструкциям в `EDGE_FUNCTIONS_SETUP.md` для развертывания Edge Functions.

**Без Edge Functions:**
- Вы сможете входить в систему
- Вы сможете просматривать и редактировать пользователей
- Но создание новых пользователей через UI не будет работать (нужно создавать вручную через Supabase Dashboard)

**С Edge Functions:**
- Все функции работают полностью
- Вы можете создавать пользователей через UI
- Вы можете сбрасывать пароли через UI

### Шаг 3: Проверьте работу
1. Войдите в систему с вашим email и паролем
2. Проверьте, что сессия сохраняется после перезагрузки
3. Попробуйте создать нового пользователя (если развернули Edge Functions)

## 📝 Примечания

- Роли теперь синхронизированы: используется `production_planner` вместо `planner`
- Все операции с пользователями теперь требуют активной сессии
- Admin API больше не используется напрямую на фронтенде (безопасно)



