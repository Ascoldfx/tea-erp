# Управление пользователями

## Где хранятся пользователи?

Пользователи хранятся в Supabase в двух таблицах:
1. **`auth.users`** - базовая информация (email, пароль, создан через Supabase Auth)
2. **`profiles`** - расширенная информация (роль, имя, склад, активность)

## Проблема: "Failed to fetch" при создании пользователя

Ошибка возникает потому, что **Edge Functions не развернуты**. Функция создания пользователей требует Edge Function `create-user`, которая не развернута на сервере.

## ✅ Решение: Создание пользователей вручную

### Способ 1: Через Supabase Dashboard (рекомендуется)

1. **Создайте пользователя в Supabase:**
   - Откройте: https://supabase.com/dashboard/project/nhsnxypdprellsmouhlp/auth/users
   - Нажмите **"Add user"** → **"Create new user"**
   - Введите:
     - **Email**: email пользователя
     - **Password**: временный пароль (минимум 6 символов)
     - **Auto Confirm User**: включите ✅
   - Нажмите **"Create User"**

2. **Назначьте роль через SQL:**
   - Откройте: https://supabase.com/dashboard/project/nhsnxypdprellsmouhlp/sql/new
   - Выполните SQL:

```sql
-- Замените 'user@example.com' на email созданного пользователя
-- Замените 'admin' на нужную роль: 'admin', 'procurement', 'production_planner', 'warehouse', 'director'
-- Замените 'Имя Пользователя' на полное имя

INSERT INTO profiles (id, email, full_name, role, is_active)
SELECT 
    id,
    email,
    'Имя Пользователя',
    'admin',  -- или другая роль
    true
FROM auth.users
WHERE email = 'user@example.com'
ON CONFLICT (id) 
DO UPDATE SET 
    role = 'admin',
    full_name = COALESCE(profiles.full_name, 'Имя Пользователя'),
    is_active = true;
```

3. **Для кладовщика добавьте склад:**
```sql
-- Если роль 'warehouse', добавьте склад
UPDATE profiles
SET warehouse_id = 'wh-main'  -- или другой ID склада
WHERE email = 'user@example.com';
```

### Способ 2: Через SQL напрямую (для опытных)

```sql
-- 1. Создайте пользователя через Supabase Auth Admin API (требует service_role)
-- Или создайте вручную через Dashboard, затем выполните:

-- 2. Обновите профиль
UPDATE profiles
SET 
    role = 'admin',
    full_name = 'Имя Пользователя',
    is_active = true
WHERE email = 'user@example.com';
```

## 📋 Роли и их права

| Роль | Права |
|------|-------|
| `admin` | Полный доступ, управление пользователями |
| `procurement` | Создание заказов, управление поставщиками |
| `production_planner` | Управление подрядчиками, планирование производства |
| `warehouse` | Приём товаров на склад, просмотр своего склада |
| `director` | Только просмотр (read-only) |

## 🔧 Развертывание Edge Functions (для будущего)

Если хотите создавать пользователей через UI, нужно развернуть Edge Functions:

1. Следуйте инструкциям в `EDGE_FUNCTIONS_SETUP.md`
2. Разверните функцию `create-user`
3. После этого создание через UI будет работать

## ✅ Проверка пользователя

После создания проверьте:

```sql
SELECT 
    u.id,
    u.email,
    u.created_at as user_created,
    p.role,
    p.is_active,
    p.full_name,
    p.warehouse_id,
    CASE 
        WHEN p.id IS NULL THEN '❌ Профиль не создан'
        WHEN p.is_active = false THEN '❌ Неактивен'
        ELSE '✅ OK'
    END as status
FROM auth.users u
LEFT JOIN profiles p ON u.id = p.id
WHERE u.email = 'user@example.com';
```

## 🆘 Частые проблемы

### Проблема: Пользователь создан, но не может войти
- Проверьте, что профиль создан: `SELECT * FROM profiles WHERE email = 'user@example.com'`
- Проверьте, что `is_active = true`
- Проверьте, что роль назначена

### Проблема: "Failed to fetch" при создании через UI
- Edge Functions не развернуты
- Используйте способ 1 (через Dashboard + SQL)

### Проблема: Пользователь не видит нужные страницы
- Проверьте роль в профиле
- Убедитесь, что роль правильная для нужных прав

