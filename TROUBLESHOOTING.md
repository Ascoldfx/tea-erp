# Устранение проблем с входом

## Проблема: Ошибка при входе, несмотря на правильные credentials

### Возможные причины:

1. ❌ Переменные окружения в Vercel все еще неправильные
2. ❌ Профиль пользователя не создан в таблице `profiles`
3. ❌ Пользователь неактивен (`is_active = false`)
4. ❌ Неправильная роль в профиле

## ✅ Решение по шагам

### Шаг 1: Проверьте переменные окружения в Vercel

1. Откройте: https://vercel.com/antons-projects-93f1a619/tea-erp/settings/environment-variables

2. Убедитесь, что:
   - `VITE_SUPABASE_URL` = `https://eopxmdywmjvpffcnlwck.supabase.co`
   - `VITE_SUPABASE_ANON_KEY` = legacy anon ключ (начинается с `eyJ...`)

3. Если значения неправильные, обновите их и сохраните

4. После сохранения Vercel автоматически перезапустит деплой

### Шаг 2: Создайте/обновите профиль пользователя

Выполните в **Supabase SQL Editor**:

```sql
-- Для пользователя ascoldfx@gmail.com
INSERT INTO profiles (id, email, full_name, role, is_active)
SELECT 
    id,
    email,
    'Антон',
    'admin',
    true
FROM auth.users
WHERE email = 'ascoldfx@gmail.com'
ON CONFLICT (id) 
DO UPDATE SET 
    role = 'admin',
    full_name = COALESCE(profiles.full_name, 'Антон'),
    is_active = true;
```

### Шаг 3: Проверьте существование профиля

Выполните в **Supabase SQL Editor**:

```sql
-- Проверка профиля
SELECT 
    p.id,
    p.email,
    p.full_name,
    p.role,
    p.is_active,
    u.email as auth_email,
    u.created_at as user_created
FROM profiles p
JOIN auth.users u ON p.id = u.id
WHERE u.email = 'ascoldfx@gmail.com';
```

Должно вернуться:
- `role` = `admin`
- `is_active` = `true`
- `email` совпадает

### Шаг 4: Проверьте в консоли браузера

1. Откройте сайт: https://tea-erp.vercel.app
2. Откройте DevTools (F12)
3. Перейдите на вкладку **Console**
4. Попробуйте войти
5. Проверьте ошибки в консоли

Возможные ошибки:
- `Failed to fetch` - проблема с переменными окружения
- `Invalid login credentials` - неправильный email/пароль
- `User profile not found` - профиль не создан
- `User profile inactive` - `is_active = false`

### Шаг 5: Проверьте Network запросы

1. В DevTools перейдите на вкладку **Network**
2. Попробуйте войти
3. Найдите запрос к Supabase (обычно к `/auth/v1/token`)
4. Проверьте:
   - Статус ответа (должен быть 200)
   - Тело ответа (должен содержать `access_token`)

## 🔍 Диагностика

### Если ошибка "Failed to fetch":

1. Проверьте переменные окружения в Vercel
2. Убедитесь, что URL правильный: `https://eopxmdywmjvpffcnlwck.supabase.co`
3. Убедитесь, что anon ключ правильный (legacy формат)

### Если ошибка "Invalid login credentials":

1. Проверьте email и пароль в Supabase Dashboard
2. Убедитесь, что пользователь создан в `auth.users`
3. Попробуйте сбросить пароль через Supabase Dashboard

### Если ошибка "User profile not found":

1. Выполните SQL из Шага 2 для создания профиля
2. Убедитесь, что профиль создан с правильным `id` (должен совпадать с `auth.users.id`)

### Если ошибка "User profile inactive":

1. Выполните SQL:
   ```sql
   UPDATE profiles 
   SET is_active = true 
   WHERE email = 'ascoldfx@gmail.com';
   ```

## 📋 Чеклист

- [ ] Переменные окружения в Vercel правильные
- [ ] Пользователь существует в `auth.users`
- [ ] Профиль создан в таблице `profiles`
- [ ] `role` = `admin` в профиле
- [ ] `is_active` = `true` в профиле
- [ ] Email совпадает в `auth.users` и `profiles`
- [ ] Пароль правильный
- [ ] Нет ошибок в консоли браузера
- [ ] Network запросы проходят успешно

## 🆘 Если ничего не помогает

1. Создайте нового пользователя в Supabase Dashboard
2. Сразу после создания выполните SQL для назначения роли
3. Попробуйте войти с новыми credentials


