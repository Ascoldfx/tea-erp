#!/bin/bash

# Скрипт для запуска dev сервера

echo "🚀 Запуск локального dev сервера..."

# Проверяем наличие .env файла
if [ ! -f .env ]; then
    echo "⚠️  Файл .env не найден. Создаю из env.txt..."
    if [ -f env.txt ]; then
        cp env.txt .env
        echo "✅ Файл .env создан"
    else
        echo "❌ Файл env.txt не найден. Создайте .env вручную."
        exit 1
    fi
fi

# Проверяем наличие node_modules
if [ ! -d "node_modules" ]; then
    echo "📦 Устанавливаю зависимости..."
    npm install
fi

# Запускаем dev сервер
echo "🌐 Запускаю Vite dev сервер..."
npm run dev

