# Исправление: Создание заказа и поиск материалов

## ✅ Что исправлено

### 1. Поиск материалов при создании заказа
**Проблема:** Поиск не работал, так как использовались моковые данные (`MOCK_ITEMS`), которых нет в базе.

**Решение:**
- Заменены моковые данные на реальные запросы к Supabase
- Материалы теперь загружаются из таблицы `items` через `inventoryService`
- Поиск работает с реальными данными из базы

### 2. Выбор поставщика
**Проблема:** Использовались моковые данные (`MOCK_CONTRACTORS`).

**Решение:**
- Поставщики теперь загружаются из таблицы `contractors` в Supabase
- Список обновляется при открытии модального окна

---

## 🚀 Что нужно сделать

### Шаг 1: Создать таблицу contractors (если еще не сделано)

1. Откройте SQL Editor в Supabase:
   https://supabase.com/dashboard/project/nhsnxypdprellsmouhlp/sql/new

2. Скопируйте и выполните содержимое файла:
   `migrations/create_contractors_table.sql`

3. После выполнения миграции:
   - Создание поставщиков через UI будет работать
   - Поставщики будут доступны при создании заказа

### Шаг 2: Проверить наличие материалов

Убедитесь, что в таблице `items` есть материалы. Если таблица пустая:

1. Используйте функцию "Seed Database" в настройках (если доступна)
2. Или добавьте материалы вручную через SQL:

```sql
INSERT INTO items (id, name, sku, unit, category)
VALUES 
    ('item-001', 'Чай черный', 'TEA-BLACK-001', 'кг', 'raw_material'),
    ('item-002', 'Упаковка 100г', 'PKG-100G', 'шт', 'packaging');
```

---

## 📋 Как теперь работает

1. **При открытии модального окна создания заказа:**
   - Автоматически загружаются материалы из таблицы `items`
   - Автоматически загружаются поставщики из таблицы `contractors`

2. **Поиск материалов:**
   - Введите название или артикул в поле "Поиск материала"
   - Список материалов фильтруется в реальном времени
   - Поиск работает по полям `name` и `sku`

3. **Выбор поставщика:**
   - Выберите поставщика из выпадающего списка
   - Список содержит всех поставщиков из таблицы `contractors`

---

## ⚠️ Важно

- Если таблица `contractors` не существует, выполните миграцию (см. Шаг 1)
- Если таблица `items` пустая, добавьте материалы
- Если поиск не работает, проверьте консоль браузера на ошибки

---

## 🔍 Проверка

После исправлений:
1. Откройте страницу "Материалы" → "Создать заказ"
2. Проверьте, что в списке поставщиков есть записи
3. Введите текст в поле "Поиск материала" - список должен фильтроваться
4. Выберите материал из списка - он должен появиться в заказе

Если что-то не работает, проверьте:
- Консоль браузера (F12) на наличие ошибок
- Что таблицы `contractors` и `items` существуют и содержат данные



