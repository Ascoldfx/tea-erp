# Логин и деплой на Vercel

## ✅ Статус: Приложение работает локально!

- Dev сервер запущен: http://localhost:5173
- Vite процесс активен
- .env файл настроен

## 🔐 Логин и пароль

### Важно: Сначала создайте пользователя в Supabase!

**Логин и пароль** - это те, которые вы создали в Supabase Authentication.

### Шаг 1: Создайте пользователя в Supabase

1. Откройте Supabase Dashboard: https://supabase.com/dashboard/project/nhsnxypdprellsmouhlp
2. Перейдите в **Authentication** → **Users**
3. Нажмите **Add User** → **Create new user**
4. Введите:
   - **Email**: `ascoldfx@gmail.com` (или любой другой)
   - **Password**: придумайте пароль (минимум 6 символов)
   - **Auto Confirm User**: включите ✅
5. Нажмите **Create User**

### Шаг 2: Назначьте роль админа

Выполните в **Supabase SQL Editor**:

```sql
INSERT INTO profiles (id, email, full_name, role)
SELECT id, email, 'Антон', 'admin'
FROM auth.users
WHERE email = 'ascoldfx@gmail.com'
ON CONFLICT (id) 
DO UPDATE SET 
    role = 'admin',
    full_name = COALESCE(profiles.full_name, 'Антон'),
    is_active = true;
```

### Шаг 3: Войдите в приложение

1. Откройте http://localhost:5173
2. Введите:
   - **Email**: `ascoldfx@gmail.com`
   - **Password**: пароль, который вы создали в Supabase
3. Нажмите **Войти**

## 🚀 Деплой на Vercel

### Почему локальная версия, а не на Vercel?

Изменения еще не закоммичены и не запушены в GitHub, поэтому Vercel не видит обновления.

### Шаг 1: Закоммитьте изменения

```bash
cd /Users/antongotskyi/Documents/cursor/tea-erp

# Добавьте все изменения
git add .

# Закоммитьте
git commit -m "feat: Add real Supabase authentication with email/password login"

# Запушьте в GitHub
git push origin main
```

### Шаг 2: Настройте переменные окружения в Vercel

1. Откройте Vercel Dashboard: https://vercel.com/antons-projects-93f1a619/tea-erp/settings/environment-variables
2. Добавьте переменные окружения:

   **Для Production, Preview и Development:**
   - `VITE_SUPABASE_URL` = `https://eopxmdywmjvpffcnlwck.supabase.co`
   - `VITE_SUPABASE_ANON_KEY` = `sb_secret_pZM7M_11N_tn5CiT89X-BQ_U334SqGP`

   ⚠️ **Важно:** Проверьте, что ключ правильный (должен быть `anon` ключ, начинается с `eyJ...`)

3. Нажмите **Save**

### Шаг 3: Деплой

После пуша в GitHub, Vercel автоматически:
1. Обнаружит изменения
2. Запустит новый деплой
3. Обновит сайт на https://tea-erp.vercel.app (или ваш домен)

Вы можете проверить статус деплоя:
- https://vercel.com/antons-projects-93f1a619/tea-erp/deployments

### Шаг 4: Проверьте деплой

После успешного деплоя:
1. Откройте ваш сайт на Vercel
2. Попробуйте войти с теми же credentials
3. Проверьте, что сессия сохраняется

## 🔍 Проверка работы

### Локально:
- ✅ http://localhost:5173 - должно работать
- ✅ Форма входа с email/password
- ✅ После входа - сессия сохраняется

### На Vercel:
- ✅ https://tea-erp.vercel.app (или ваш домен)
- ✅ Те же функции, что и локально
- ✅ Переменные окружения настроены

## ⚠️ Важные замечания

1. **Ключ Supabase**: Убедитесь, что используете `anon` ключ (публичный), а не `service_role` (секретный)
   - Получить можно: Supabase Dashboard → Project Settings → API → Project API Keys → `anon` `public`

2. **Первый пользователь**: Должен быть создан в Supabase Dashboard, затем назначена роль через SQL

3. **Edge Functions**: Для создания пользователей через UI нужны Edge Functions (см. `EDGE_FUNCTIONS_SETUP.md`)

## 📝 Быстрая проверка

```bash
# Проверить локальный сервер
curl http://localhost:5173

# Проверить git статус
git status

# Проверить последний коммит
git log -1
```

## 🆘 Если что-то не работает

### Проблема: "Invalid login credentials"
- Проверьте, что пользователь создан в Supabase
- Проверьте, что email и пароль правильные
- Проверьте, что профиль создан с правильной ролью

### Проблема: "Supabase not initialized"
- Проверьте `.env` файл локально
- Проверьте переменные окружения в Vercel

### Проблема: Vercel не видит изменения
- Убедитесь, что изменения запушены в GitHub
- Проверьте, что Vercel подключен к правильному репозиторию
- Проверьте логи деплоя в Vercel Dashboard



