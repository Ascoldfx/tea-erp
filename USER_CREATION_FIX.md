# Создание пользователей через UI

## Проблема

При попытке создать пользователя через UI получается ошибка "Failed to fetch".

## Причина

Создание пользователей требует **Edge Functions**, которые не развернуты на сервере. Функция `create-user` пытается вызвать `/functions/v1/create-user`, но эта функция не существует на сервере.

## ✅ Решение

### Вариант 1: Развернуть Edge Functions (рекомендуется для будущего)

Следуйте инструкциям в `EDGE_FUNCTIONS_SETUP.md`:

1. Установите Supabase CLI
2. Войдите: `npx supabase login`
3. Свяжите проект: `npx supabase link --project-ref nhsnxypdprellsmouhlp`
4. Установите секреты: `npx supabase secrets set SUPABASE_SERVICE_ROLE_KEY=your_key`
5. Разверните функции: `npx supabase functions deploy create-user`

После этого создание через UI будет работать.

### Вариант 2: Создание через Supabase Dashboard (текущее решение)

**Это единственный способ, который работает сейчас:**

1. **Создайте пользователя:**
   - Откройте: https://supabase.com/dashboard/project/nhsnxypdprellsmouhlp/auth/users
   - Нажмите **"Add user"** → **"Create new user"**
   - Введите email и пароль
   - Включите **"Auto Confirm User"** ✅

2. **Назначьте роль через SQL:**
   - Откройте: https://supabase.com/dashboard/project/nhsnxypdprellsmouhlp/sql/new
   - Выполните:

```sql
INSERT INTO profiles (id, email, full_name, role, is_active)
SELECT 
    id,
    email,
    'Имя Пользователя',
    'admin',  -- или другая роль
    true
FROM auth.users
WHERE email = 'user@example.com'
ON CONFLICT (id) 
DO UPDATE SET 
    role = 'admin',
    full_name = 'Имя Пользователя',
    is_active = true;
```

## 📋 Почему так?

- Создание пользователей требует `service_role` ключ
- `service_role` ключ нельзя использовать на фронтенде (безопасность)
- Edge Functions работают на сервере и могут использовать `service_role`
- Пока Edge Functions не развернуты, используйте Dashboard + SQL

## 🔧 Альтернатива: Упрощенное создание через UI

Можно создать упрощенную версию, которая:
1. Создает пользователя через обычный `signUp` API
2. Затем админ обновляет роль через UI

Но это менее удобно, так как требует двух шагов.

## ✅ Рекомендация

**Сейчас:** Используйте Supabase Dashboard + SQL для создания пользователей.

**В будущем:** Разверните Edge Functions для полноценного создания через UI.

Подробная инструкция в `USER_MANAGEMENT_GUIDE.md`.


